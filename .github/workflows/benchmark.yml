name: Build performance benchmarks

# On-demand only: build-performance numbers are environment-sensitive, so this is
# never run automatically. Trigger it from the Actions tab; results are written to
# a per-OS text file, printed into the run summary and uploaded as an artifact.
# These runs are the figures: a laptop measures a laptop, a workflow anyone can
# re-trigger measures something a reader can reproduce. The default set produces
# every table benchmark.sh knows except the full (with-tests) build, which is slow
# and gated behind the `full` switch.
on:
  workflow_dispatch:
    inputs:
      tables:
        description: 'Tables to run (space-separated subset of: launch make compile maven pinning aot bootstrap)'
        default: 'launch make compile maven pinning aot bootstrap'
      runners:
        description: 'Runners to measure, as a JSON array - narrow it to chase one platform'
        default: '["ubuntu-latest","macos-latest","windows-latest"]'
      full:
        description: 'Also run the full build with the whole test suite (slow, adds ~15-20 min per runner)'
        type: boolean
        default: false
      maven3_version:
        description: 'Maven 3 version to download for the Maven baseline'
        default: '3.9.9'
      maven4_version:
        description: 'Maven 4 version to download for the maven3-vs-4 table'
        default: '4.0.0-rc-6'
      runs_cold:
        description: 'Repetitions per cold scenario'
        default: '5'
      runs_warm:
        description: 'Repetitions per warm scenario'
        default: '3'

# Route Maven Central through the repository variable MAVEN_REPOSITORY_URI when set
# (the Google Cloud mirror of Central, to avoid Central rate-limiting the runners),
# falling back to Central itself so an unset variable never becomes an empty URI.
env:
  MAVEN_REPOSITORY_URI: ${{ vars.MAVEN_REPOSITORY_URI || 'https://repo1.maven.org/maven2/' }}

jobs:
  benchmark:
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(inputs.runners) }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # GraalVM gives both a HotSpot `java` (for the source, precompiled and AOT
      # scenarios) and `native-image` (for the native launcher column); it also
      # exports GRAALVM_HOME, which benchmark.sh reads to build the native image.
      - name: Set up GraalVM JDK 25
        uses: graalvm/setup-graalvm@329c42c5f4c343bceb505f0b28cc8499bc2bf174 # v1.5.4
        with:
          distribution: graalvm
          java-version: '25'

      # Fold the `full` switch into the table list so the rest of the job reads a
      # single TABLES value.
      - name: Resolve tables
        shell: bash
        run: |
          tables="${{ inputs.tables }}"
          if [ "${{ inputs.full }}" = "true" ]; then
            case " $tables " in *" full "*) ;; *) tables="$tables full";; esac
          fi
          echo "TABLES=$tables" >> "$GITHUB_ENV"

      # Pin both Maven majors rather than relying on the runner's bundled `mvn`,
      # whose exact version drifts with the image: Maven 3 is the baseline every
      # Maven-comparing table runs, so install it unconditionally; Maven 4 is only
      # needed by the maven3-vs-4 table. benchmark.sh reads the launchers through
      # MVN and MVN4; the unix `bin/mvn` script is used on every OS (Git Bash on
      # Windows runs it like Maven 3's launcher).
      - name: Install Maven 3
        shell: bash
        env:
          MAVEN3_VERSION: ${{ inputs.maven3_version }}
        run: |
          dir="$RUNNER_TEMP/maven3"
          # On Windows $RUNNER_TEMP is a `D:\...` path; cygpath rewrites it to a
          # POSIX path so tar does not read the `D:` drive letter as a remote host.
          command -v cygpath >/dev/null && dir="$(cygpath -u "$dir")"
          mkdir -p "$dir"
          base="apache-maven-$MAVEN3_VERSION-bin.tar.gz"
          curl -fsSL --retry 3 "https://dlcdn.apache.org/maven/maven-3/$MAVEN3_VERSION/binaries/$base" -o "$dir/$base" \
            || curl -fsSL --retry 3 "https://archive.apache.org/dist/maven/maven-3/$MAVEN3_VERSION/binaries/$base" -o "$dir/$base"
          tar -xzf "$dir/$base" -C "$dir"
          echo "MVN=$dir/apache-maven-$MAVEN3_VERSION/bin/mvn" >> "$GITHUB_ENV"
          "$dir/apache-maven-$MAVEN3_VERSION/bin/mvn" -version

      - name: Install Maven 4
        if: contains(env.TABLES, 'maven')
        shell: bash
        env:
          MAVEN4_VERSION: ${{ inputs.maven4_version }}
        run: |
          dir="$RUNNER_TEMP/maven4"
          command -v cygpath >/dev/null && dir="$(cygpath -u "$dir")"
          mkdir -p "$dir"
          base="apache-maven-$MAVEN4_VERSION-bin.tar.gz"
          curl -fsSL --retry 3 "https://dlcdn.apache.org/maven/maven-4/$MAVEN4_VERSION/binaries/$base" -o "$dir/$base" \
            || curl -fsSL --retry 3 "https://archive.apache.org/dist/maven/maven-4/$MAVEN4_VERSION/binaries/$base" -o "$dir/$base"
          tar -xzf "$dir/$base" -C "$dir"
          echo "MVN4=$dir/apache-maven-$MAVEN4_VERSION/bin/mvn" >> "$GITHUB_ENV"
          "$dir/apache-maven-$MAVEN4_VERSION/bin/mvn" -version

      # Populate the caches the offline benchmarks rely on: a full Jenesis build
      # primes its resolution cache; a Maven build primes ~/.m2 so the `-o`
      # (offline) Maven scenarios resolve - with tests when the full table is
      # requested (it needs the test-execution plugins offline), otherwise the
      # faster -DskipTests warm that the compile/maven tables match. Maven 4 is
      # primed separately because it binds some plugin versions of its own.
      - name: Warm caches
        shell: bash
        run: |
          java -Djenesis.dependency.pin=strict -Djenesis.project.layout=maven build/jenesis/Make.java build
          case " $TABLES " in
            *" full "*) "$MVN" -B -ntp package ;;
            *" compile "*|*" maven "*) "$MVN" -B -ntp -DskipTests package ;;
          esac
          if [ -n "${MVN4:-}" ]; then
            case " $TABLES " in *" maven "*) "$MVN4" -B -ntp -DskipTests package ;; esac
          fi

      - name: Run benchmarks
        shell: bash
        env:
          RUNS_COLD: ${{ inputs.runs_cold }}
          RUNS_WARM: ${{ inputs.runs_warm }}
          MAVEN_VERSION: ${{ inputs.maven3_version }}
        run: |
          out="benchmark-${{ matrix.os }}.txt"
          {
            echo "Jenesis build-performance benchmarks"
            echo "os:     ${{ matrix.os }} (${{ runner.arch }})"
            echo "tables: $TABLES"
            echo "runs:   cold=$RUNS_COLD warm=$RUNS_WARM"
            echo "date:   $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            "$MVN" -version 2>&1 | grep -m1 'Apache Maven' | sed 's/^/maven3: /'
            [ -n "${MVN4:-}" ] && "$MVN4" -version 2>&1 | grep -m1 'Apache Maven' | sed 's/^/maven4: /'
            java -version 2>&1 | sed 's/^/java:   /'
            echo
          } | tee "$out"
          for t in $TABLES; do
            bash benchmark/benchmark.sh "$t" 2>&1 | tee -a "$out"
          done

      # The run page carries the tables itself, so a reader compares runners without
      # downloading three artifacts and a re-trigger is the whole reproduction recipe.
      - name: Publish results to the run summary
        if: always()
        shell: bash
        run: |
          out="benchmark-${{ matrix.os }}.txt"
          [ -f "$out" ] || exit 0
          {
            echo "### ${{ matrix.os }} (${{ runner.arch }})"
            echo '```'
            cat "$out"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: benchmark-${{ matrix.os }}
          path: benchmark-${{ matrix.os }}.txt
          if-no-files-found: warn
