/**
 * Jenesis Publishing Demo.
 *
 * A sample library whose name and description are taken from this Javadoc and
 * whose Maven coordinate is derived from the module name, so only the metadata a
 * module declaration cannot express - project URL, licenses, developers, SCM - is
 * supplied through project.properties.
 *
 * @jenesis.release 25
 */
module build.jenesis.demo.publishing {
    exports sample;
}
