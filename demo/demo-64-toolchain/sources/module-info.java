/**
 * A module that names the JDK its build runs on, and prints the feature version of
 * the JDK that runs it.
 *
 * @jenesis.main demo.toolchain.Feature
 */
module demo.toolchain {
    exports demo.toolchain;
}
