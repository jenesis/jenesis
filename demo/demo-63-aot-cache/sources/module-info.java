/**
 * Jenesis Ahead-of-Time Cache Demo.
 *
 * A one-class module, so that a build spends its time starting rather than
 * compiling, which is what a cache of the engine's loaded classes removes.
 *
 * @jenesis.release 25
 * @jenesis.main sample.app.App
 */
module sample.app {
    exports sample.app;
}
