# Presence of this file makes the modular layouts emit the module's resolved
# dependency closure as a BOM, published into the local Jenesis repository as
# <module>/<version>/<module>.properties alongside the module jar.
