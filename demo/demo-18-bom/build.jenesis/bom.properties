# Presence of this file makes the modular layouts emit the module's resolved
# dependency closure as a BOM, published into the local Jenesis repository
# alongside the module jar: <module>/<version>/<module>.properties, or
# <module>/<module>.properties where the module names no version.
