/**
 * A module with nothing to resolve, built to show that its jar comes out byte for
 * byte the same wherever it is built.
 *
 * @jenesis.release 25
 */
module demo.reproducible {
    exports demo.reproducible;
}
